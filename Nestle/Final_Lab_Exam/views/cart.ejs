<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Cart - Nestle Pakistan</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <style>
        /* Header Styling */
        .header {
            background-color: #00796b;
            color: white;
            padding: 20px 0;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-radius: 5px;
            margin-bottom: 30px;
        }

        .header h1 {
            font-family: 'Arial', sans-serif;
            font-size: 2rem;
            margin-left: 20px;
        }

        .header .btn-products {
            background-color: #ffffff;
            color: #00796b;
            border: 2px solid #ffffff;
            padding: 10px 20px;
            border-radius: 5px;
            text-transform: uppercase;
            font-weight: bold;
            margin-right: 20px;
            transition: background-color 0.3s, color 0.3s;
        }

        .header .btn-products:hover {
            background-color: #004d40;
            color: white;
            border-color: #004d40;
        }

        /* Container Styling */
        .container {
            margin-top: 30px;
            max-width: 90%;
        }

        h4 {
            text-align: center;
            margin-bottom: 30px;
            font-family: 'Arial', sans-serif;
            color: #2c3e50;
        }

        /* Table Styling */
        .table {
            width: 100%;
            margin: 0 auto;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            border: 1px solid #ddd;
        }

        .table th, .table td {
            text-align: center;
            padding: 15px;
            font-size: 1.1rem;
        }

        .table thead {
            background-color: #00796b;
            color: white;
            font-weight: bold;
        }

        .table tbody tr:hover {
            background-color: #e0f2f1;
        }

        /* Button Styling */
        .btn-danger {
            background-color: #f44336;
            border-color: #f44336;
            font-size: 1rem;
            padding: 8px 16px;
            border-radius: 5px;
            transition: background-color 0.3s, transform 0.3s;
            text-transform: uppercase;
        }

        .btn-danger:hover {
            background-color: #d32f2f;
            transform: scale(1.05);
        }

        .btn-primary {
            background-color: #00796b;
            border-color: #00796b;
            font-size: 1.1rem;
            padding: 12px 18px;
            border-radius: 5px;
            transition: background-color 0.3s, transform 0.3s;
            text-transform: uppercase;
        }

        .btn-primary:hover {
            background-color: #004d40;
            transform: scale(1.05);
        }

        /* Buttons Container */
        .btn-container {
            display: flex;
            justify-content: flex-end;
            margin-top: 30px;
        }

        .btn-container a {
            margin-left: 15px;
        }

        /* Cart row styling */
        .cart-row {
            background-color: #f9f9f9;
            border-radius: 8px;
            margin-bottom: 15px;
            box-shadow: 0 3px 6px rgba(0, 0, 0, 0.1);
        }

        .cart-row:hover {
            background-color: #e8f5e9;
        }
    </style>
</head>
<body>
    <!-- Header -->
    <div class="header">
        <h1>Nestle Pakistan</h1>
        <a href="/homepage" class="btn btn-products">Products</a>
    </div>

    <div class="container">
        <h4>My Cart</h4>
        <table class="table">
            <thead>
                <tr>
                    <th>Product</th>
                    <th>Title</th>
                    <th>Price</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <% products.forEach(product => { %>
                    <tr class="cart-row">
                        <td>
                            <img src="<%= product.picture %>" alt="<%= product.title %>" style="width: 100px; height: auto; border-radius: 8px;"/>
                        </td>
                        <td><%= product.title %></td>
                        <td><%= product.price %> PKR</td>
                        <td>
                            <form action="/cart/remove/<%= product._id %>" method="POST" style="display:inline;">
                                <button type="submit" class="btn btn-danger">Remove</button>
                            </form>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
        
        <div class="btn-container">
            <a href="/Checkout" class="btn btn-primary">Checkout</a>
        </div>
    </div>
</body>
</html>
